---
import type { TimelineProps as Props } from '../../types/education';
import SectionTitle from '../SectionTitle.astro';
import EducationItem from "./components/EducationItem.astro";

const {education} = Astro.props



---
<SectionTitle id='education-title' eyebrow='Educación'  title='Formación y Certificados'>
    
</SectionTitle>

<ol class="relative border-s border-default flex flex-col gap-7 ">   
     {education.map((ed, i) => (
  <EducationItem education={ed} />
))}
</ol>



