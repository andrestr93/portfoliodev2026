---
const {class: className , id} = Astro.props
import { fade } from "astro:transitions";
---

<section  transition:animate= {fade({ duration: '0.4s' })} id={id} class={`w-full mx-auto lg:w-185 flex flex-col items-center justify-center   ${className}`}>

    <slot/>

</section>